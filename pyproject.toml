[project]
name = "amundworks"
version = "0.1.0"
description = "AmundWorks CLI tools"
authors = [{name = "Simon Stordal Amundgård"}]
dependencies = [
    "prompt_toolkit",
    "numpy"
]

[project.scripts]
uniforge = "UniForge.UniForgeCLI:_mainEnv"

[tool.setuptools]
packages = ["awpc", "UniForge"]