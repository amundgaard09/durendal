[project]
name = "AWPC" # AWPC for now. Will fix with polyrepo architechture
version = "0.0.1.2"
description = "AmundWorks Multi-dicipline Engineering"
authors = [{name = "Simon Stordal Amundgård"}]
dependencies = [
    "prompt_toolkit",
    "numpy"
]

[tool.setuptools]
packages = [
    "awpc",
    #"uniforge",
    #"vulcan",
]